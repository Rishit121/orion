# orion:

Orion is a Retrieval-Augmented Generation (RAG) system that converts large documents (like PDFs) into a conversational AI interface. It uses Azure OpenAI, LangChain, and FAISS to enable semantic search and context-aware question answering.
